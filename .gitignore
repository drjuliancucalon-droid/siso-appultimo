# Dependencies
node_modules/
.yarn/

# Build output
dist/
build/

# Vite cache
.vite/

# Environment variables (NEVER commit these)
.env
.env.local
.env.production
.env.staging
backend/.env

# Editor
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db
desktop.ini

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Testing
coverage/
.nyc_output/

# Temp
*.tmp
*.bak

# Vite build logs
vite_log.txt
vite_out2.txt
vite_output.txt

# Clones de referencia del monolito usados para auditorías — NUNCA deben
# trackearse como submódulo (rompe el deploy de Cloudflare Pages, ya pasó dos
# veces: commits 0857df3 y 11eb7d8). La entrada anterior tenía comillas
# literales en el patrón, por lo que nunca hizo match con el nombre real.
ocupasaludparadesplegar-forense/
mono-original/
mono-real/
mono-temp/

# Basura de scratch/debug que se coló en commits de auditoría — no aporta
# nada al código y no debería vivir en el repo.
diff_*.patch
temp_mono_*.txt
temp_refactor_*.txt
__mono_*.txt
PROTOCO
SESION

# Worktrees de agentes (copias completas del repo para trabajo aislado) —
# nunca deben trackearse ni ejecutarse como parte de la suite de tests.
.claude/
